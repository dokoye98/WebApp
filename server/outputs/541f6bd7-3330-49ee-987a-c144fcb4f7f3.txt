Ready to receive input:
Token{key=VARIABLE, value='wolf'}
Token{key=VAR_ASSIGN, value='='}
Token{key=LITERAL, value='sham'}
AST after optimization: VariableAssignment : void
  |
  +-- Variable(wolf)
  +-- Literal("sham") : String
wolf = "sham"
t0 = wolf
LOAD R0, "sham"
STORE R0, wolf
Registers
R0 = sham
Variables:
wolf = sham
Assembly Code here:

null
Token{key=PRINT, value='print'}
Token{key=LITERAL, value='wolf'}
AST after optimization: PrintStatement : void
  |
  +-- Literal("wolf") : String
t0 = "wolf"
call print, t0
LOAD R0, "wolf"
CALL print, R0
wolf
Registers
R0 = wolf
Variables:
Assembly Code here:

section .data
message db  'wolf', 0;

section .bss
;

section .text
global _start            ;
_start
_start:
    mov eax, message
    push eax
    call print
    add esp, 4
    mov eax, 1          ; exit call
    xor ebx, ebx        ; exit code 0
    int 0x80            ;  system call
print:
    mov edx, len        ; dynamic string len
    mov ecx, [esp+4]    ; 
    mov ebx, 1          ; File descriptor (stdout)
    mov eax, 4          ;  call number -> sys_write
    int 0x80            ; system call
    ret                 ; Return from the function
section .data
len equ $ - message     ;length is automatically calculated
Finished processing input.
